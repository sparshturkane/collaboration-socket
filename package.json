{
  "name": "socket-server",
  "version": "1.0.0",
  "description": "socket server to serve a chat application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sparsh turkane",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
